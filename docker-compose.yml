services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: xpr_quests
      POSTGRES_USER: quests
      POSTGRES_PASSWORD: quests_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  pgdata:
